# The frontend currently combines ESLint 10 / typescript-eslint 8 with
# legacy Airbnb shareable configs whose peer ranges have not caught up yet.
# Keep npm ci reproducible across local builds, GitHub Actions, Railway, and Azure.
legacy-peer-deps=true
fund=false
audit=false
